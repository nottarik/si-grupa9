# Kopiraj ovaj fajl u .cloudflared.env i upiši token.
# Token dobijaš u: Cloudflare Zero Trust → Networks → Tunnels → tvoj tunel → Configure → Install connector
# Nikad ne commitovati .cloudflared.env (gitignored).

TUNNEL_TOKEN=TVOJ_TUNNEL_TOKEN_OVDJE
