# Heavy ML/AI deps live in a separate file so Docker caches them in their own layer.
-r requirements-ml.txt

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Data validation
pydantic[email]==2.7.1
pydantic-settings==2.6.1

# IANA timezone database for zoneinfo (Europe/Sarajevo schedule times)
tzdata==2024.1

# Auth
python-jose[cryptography]==3.3.0
bcrypt==4.2.0
python-multipart==0.0.9

# Database
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Redis (optional — Upstash cache, rate limiting)
redis==5.0.4

# AI / RAG  (langchain, langchain-community, sentence-transformers → requirements-ml.txt)
groq==0.8.0
qdrant-client==1.9.1

# Cloud Storage
supabase==2.9.0

# Google Drive batch import
google-api-python-client==2.137.0
google-auth==2.32.0

# HTTP client
httpx==0.27.0

# Utilities
python-dotenv==1.0.1
aiofiles==23.2.1
pdfplumber==0.11.4

# Preprocessing / PII masking  (spacy → requirements-ml.txt)
cryptography==42.0.8

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0
aiosqlite==0.20.0
